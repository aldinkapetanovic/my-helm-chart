replicaCount: 2

image:
  repository: nginx
  tag: 1.21.1
  pullPolicy: IfNotPresent

service:
  name: mychart-service
  port: 80
  type: LoadBalancer

# ingress:
#   enabled: true
#   annotations: {}
#   path: /

persistence:
  enabled: true
  storageClassName: manual
  accessMode: ReadWriteOnce
  size: 5Gi
